package com.tacker.tracker.repository;

import com.tacker.tracker.models.Accelerator;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AcceleratorRepository extends JpaRepository<Accelerator, Integer> {
}
